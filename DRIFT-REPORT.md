# Token drift report

Generated by `scripts/audit-drift.mjs` against 46 stylesheets in `src/styles`.

**0 off-scale** — the value is not on any scale. Each needs a decision.  
**0 no-role** — on the primitive scale, but no semantic token names it.  
**0 negated** — a token value with a minus sign; needs `calc(-1 * var(…))`.  
**0 unlinked** — already the right value, written as a literal. `scripts/codemod-tokens.mjs` rewrites these.

None of these are bugs today; the UI renders correctly. They are the places a theme switch, a density change or a rebrand would miss, and where the system currently claims a guarantee it cannot keep.

## font-size — clean

Target: `--bh-text-size-*`. Declared values: 11px, 13px, 14px, 16px, 20px.

Nothing to do.

## font-weight — clean

Target: `--bh-text-weight-*`. Declared values: 400, 500, 600.

Nothing to do.

## spacing — clean

Target: `--bh-gap-* / --bh-padding-*`. Declared values: 12px, 16px, 24px, 2px, 32px, 48px, 4px, 8px, 96px.

Nothing to do.

## border-radius — clean

Target: `--bh-radius-*`. Declared values: 10px, 3px, 6px, 9999px.

Nothing to do.

## inset — clean

Target: `--bh-gap-* / a percentage`. Declared values: 12px, 16px, 24px, 2px, 32px, 48px, 4px, 8px, 96px.

A literal inset usually encodes a relationship to something else’s size. Prefer centring (top: 50% + translate) or a gap token over a number that was correct once.

Nothing to do.

## duration — clean

Target: `--bh-duration-*`. Declared values: 1400ms, 140ms, 220ms, 320ms, 700ms, 80ms.

Nothing to do.

## easing — clean

Target: `--bh-easing-*`. Declared values: cubic-bezier(0, 0, 0.2, 1), cubic-bezier(0, 0, 1, 1), cubic-bezier(0.2, 0, 0, 1), cubic-bezier(0.3, 0, 1, 1).

Nothing to do.

## z-index — clean

Target: `--bh-layer-*`. Declared values: 0, 10, 100, 1000, 30, 40.

Values from -4 to 4 are exempt: sibling ordering inside one component is a local claim, and naming it globally would be worse.

Nothing to do.

