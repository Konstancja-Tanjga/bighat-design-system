{
  "name": "@bighatpoland/ui",
  "version": "3.2.0",
  "skill": "./agent/SKILL.md",
  "storybook": "https://bighatpoland.github.io/bighat-design-system/",
  "tokens": {
    "layers": [
      "primitive",
      "semantic"
    ],
    "productApi": "semantic",
    "cssPrefix": "--bh",
    "note": "Product code and generated code may reference semantic tokens only. Primitives exist to define semantics and are not part of the public API.",
    "semanticGroups": [
      "surface",
      "text",
      "border",
      "action",
      "status",
      "control",
      "gap",
      "padding",
      "radius",
      "elevation",
      "textSize",
      "textLeading",
      "breakpoint"
    ],
    "contrast": {
      "standard": "WCAG 2.1 AA",
      "bodyText": 4.5,
      "largeText": 3,
      "nonText": 3,
      "enforcedBy": "src/tokens/contrast.test.ts \u2014 runs in CI, fails the build"
    },
    "breakpoints": {
      "values": {
        "sm": "480px",
        "md": "900px",
        "lg": "1200px"
      },
      "note": "Container widths, not viewport widths. A component responds to the space it was given \u2014 the same Table sits in main at 1200px, in a SidePanel at 432px and in a Card at 280px on one screen. Component CSS uses @container, and the literals are duplicated there because CSS cannot read a custom property inside a query.",
      "enforcedBy": "src/tokens/breakpoints.test.ts \u2014 walks every stylesheet, fails the build on any query threshold that is not a declared token"
    }
  },
  "components": [
    {
      "name": "Accordion",
      "purpose": "Disclosure of one topic at a time. Headers are real buttons inside headings.",
      "notFor": [
        "Navigation between screens",
        "Hiding content the user always needs"
      ],
      "props": {
        "multiple": {
          "type": "boolean",
          "default": false
        },
        "defaultOpen": {
          "type": "string[]",
          "note": "Ids open on first render."
        },
        "headingLevel": {
          "type": "2 | 3 | 4 | 5 | 6",
          "default": 3,
          "note": "Where the accordion sits in the document outline."
        },
        "AccordionItem.title": {
          "type": "ReactNode",
          "required": true
        },
        "AccordionItem.meta": {
          "type": "ReactNode",
          "note": "Count or status next to the title."
        }
      },
      "exports": [
        "Accordion",
        "AccordionItem"
      ]
    },
    {
      "name": "AppBar",
      "purpose": "The top bar. Three slots in reading order: brand, title, actions.",
      "notFor": [
        "Repeating the product name in title \u2014 brand already carries it"
      ],
      "props": {
        "brand": {
          "type": "ReactNode",
          "note": "Logo or wordmark."
        },
        "title": {
          "type": "ReactNode",
          "note": "Names the current screen, not the product."
        },
        "titleAsHeading": {
          "type": "boolean",
          "default": true,
          "note": "false when the screen has its own visible <h1>."
        },
        "center": {
          "type": "ReactNode",
          "note": "Search or filters acting on this screen."
        },
        "actions": {
          "type": "ReactNode",
          "note": "Primary action last."
        }
      }
    },
    {
      "name": "AppShell",
      "purpose": "The frame every application screen sits in. Owns the landmark structure \u2014 header, nav, main, complementary.",
      "notFor": [
        "Documentation and marketing pages \u2014 use height=\"flow\" or no shell at all"
      ],
      "props": {
        "header": {
          "type": "ReactNode",
          "note": "Rendered as <header>."
        },
        "rail": {
          "type": "ReactNode",
          "note": "Narrow icon rail on the leading edge."
        },
        "sidebar": {
          "type": "ReactNode",
          "note": "Leading panel \u2014 navigation, a tree."
        },
        "aside": {
          "type": "ReactNode",
          "note": "Trailing panel \u2014 inspector, context."
        },
        "children": {
          "type": "ReactNode",
          "required": true,
          "note": "Becomes the only <main>."
        },
        "height": {
          "type": "'fill' | 'flow'",
          "default": "fill",
          "meaning": "fill pins to the viewport and scrolls each region independently"
        },
        "navOpen": {
          "type": "boolean",
          "default": false,
          "note": "Below breakpoint.md the rail and sidebar leave the grid; with onNavToggle they return as an overlay instead of simply vanishing."
        },
        "onNavToggle": {
          "type": "() => void",
          "note": "Omit and the panels stay hidden below the breakpoint \u2014 honest, but the reader has no route back to them."
        },
        "asideOpen": {
          "type": "boolean",
          "default": false
        },
        "onAsideToggle": {
          "type": "() => void"
        }
      },
      "exports": [
        "AppShell",
        "SkipLink"
      ],
      "note": "Slots are optional. A screen without a rail is this component with a prop left out, not a second layout."
    },
    {
      "name": "Avatar",
      "purpose": "A person, as a picture with a name behind it.",
      "notFor": [
        "A logo",
        "Carrying status on its own"
      ],
      "props": {
        "name": {
          "type": "string",
          "required": true,
          "note": "Initials and accessible name come from it."
        },
        "src": {
          "type": "string"
        },
        "size": {
          "type": "'sm' | 'md' | 'lg'",
          "default": "md"
        },
        "decorative": {
          "type": "boolean",
          "default": false,
          "note": "True when the name is already visible next to it."
        },
        "AvatarGroup.label": {
          "type": "string",
          "required": true
        },
        "AvatarGroup.max": {
          "type": "number",
          "default": 4
        }
      },
      "exports": [
        "Avatar",
        "AvatarGroup"
      ]
    },
    {
      "name": "Badge",
      "purpose": "Short status attached to an object.",
      "notFor": [
        "Bare counts",
        "Anything clickable"
      ],
      "props": {
        "tone": {
          "type": "'neutral' | 'info' | 'success' | 'warning' | 'critical'",
          "default": "neutral"
        },
        "dot": {
          "type": "boolean",
          "default": false,
          "note": "Decorative. The label still carries the meaning."
        },
        "children": {
          "type": "ReactNode",
          "required": true,
          "note": "Required by design \u2014 colour alone fails WCAG 1.4.1."
        }
      }
    },
    {
      "name": "Board",
      "purpose": "Columns of cards \u2014 kanban, pipeline, review queue.",
      "notFor": [
        "Ordered data the user compares \u2014 that is Table"
      ],
      "props": {
        "Board.ariaLabel": {
          "type": "string",
          "required": true
        },
        "Board.announcement": {
          "type": "string",
          "note": "Spoken by the board's live region after a move."
        },
        "BoardColumn.title": {
          "type": "string",
          "required": true
        },
        "BoardColumn.count": {
          "type": "number",
          "required": true
        },
        "BoardColumn.limit": {
          "type": "number",
          "note": "WIP limit \u2014 surfaced, never enforced."
        },
        "BoardColumn.empty": {
          "type": "ReactNode",
          "note": "Use a StateBlock with copy specific to the column."
        },
        "BoardCard.moveTargets": {
          "type": "Array<{id,label}>"
        },
        "BoardCard.onMove": {
          "type": "(targetId: string) => void",
          "note": "Required for a pointer-free move \u2014 WCAG 2.5.1."
        }
      }
    },
    {
      "name": "Breadcrumbs",
      "purpose": "Position in a hierarchy. The last crumb is the current page and is not a link.",
      "notFor": [
        "Click history",
        "A two-level hierarchy \u2014 a back link is enough"
      ],
      "props": {
        "items": {
          "type": "BreadcrumbItem[]",
          "required": true
        },
        "ariaLabel": {
          "type": "string",
          "default": "Breadcrumb"
        },
        "maxItems": {
          "type": "number",
          "default": 5,
          "note": "Beyond this the middle collapses; root and leaf always show."
        }
      }
    },
    {
      "name": "Button",
      "purpose": "An action the user takes.",
      "notFor": [
        "Navigation \u2014 use an anchor styled as a link"
      ],
      "props": {
        "variant": {
          "type": "'primary' | 'secondary' | 'ghost'",
          "default": "primary",
          "meaning": "visual weight"
        },
        "tone": {
          "type": "'default' | 'critical'",
          "default": "default",
          "meaning": "consequence of the action"
        },
        "size": {
          "type": "'sm' | 'md' | 'lg'",
          "default": "md"
        },
        "loading": {
          "type": "boolean",
          "default": false,
          "note": "Stays focusable; blocks the click."
        },
        "loadingLabel": {
          "type": "string",
          "default": "Loading"
        },
        "iconStart": {
          "type": "ReactNode",
          "note": "Decorative \u2014 hidden from assistive tech."
        },
        "iconEnd": {
          "type": "ReactNode"
        },
        "fullWidth": {
          "type": "boolean",
          "default": false
        }
      },
      "removed": {
        "variant=\"danger\"": {
          "replacement": "tone=\"critical\"",
          "removedIn": "3.0.0",
          "deprecatedIn": "2.0.0",
          "reason": "variant conflated visual weight with consequence"
        }
      }
    },
    {
      "name": "Card",
      "purpose": "A bounded surface around content the user might act on as a unit.",
      "notFor": [
        "A paragraph \u2014 that is a border with extra steps",
        "Cards with two actions inside"
      ],
      "props": {
        "children": {
          "type": "ReactNode",
          "required": true
        },
        "onClick": {
          "type": "() => void",
          "note": "Makes the whole surface one button."
        },
        "ariaLabel": {
          "type": "string",
          "note": "Its accessible name when onClick is set."
        },
        "elevation": {
          "type": "'flat' | 'raised'",
          "default": "flat"
        },
        "accent": {
          "type": "'none' | 'info' | 'success' | 'warning' | 'critical'",
          "default": "none",
          "note": "Decorative \u2014 never the only carrier of a status."
        },
        "padding": {
          "type": "'snug' | 'normal'",
          "default": "normal"
        },
        "dragging": {
          "type": "boolean",
          "default": false
        }
      }
    },
    {
      "name": "Checkbox",
      "purpose": "An independent yes/no that is submitted with a form.",
      "notFor": [
        "An immediate setting \u2014 that is Switch",
        "Mutually exclusive options \u2014 that is RadioGroup"
      ],
      "props": {
        "label": {
          "type": "ReactNode",
          "required": true
        },
        "description": {
          "type": "ReactNode"
        },
        "error": {
          "type": "string",
          "note": "Its presence is the invalid state."
        },
        "indeterminate": {
          "type": "boolean",
          "default": false,
          "note": "Set as a DOM property, so it announces as mixed."
        }
      }
    },
    {
      "name": "Combobox",
      "purpose": "A text field that filters a list. The one custom listbox in the system.",
      "notFor": [
        "A short, fixed set \u2014 use Select",
        "Free text \u2014 the value must come from the list"
      ],
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "options": {
          "type": "ComboboxOption[]",
          "required": true
        },
        "value": {
          "type": "string | null",
          "required": true
        },
        "onChange": {
          "type": "(value: string | null) => void",
          "required": true
        },
        "filter": {
          "type": "(option, query) => boolean",
          "note": "Defaults to case-insensitive contains."
        },
        "emptyMessage": {
          "type": "string",
          "default": "No matches"
        }
      },
      "note": "Escape closes the list; a second Escape clears the field. Blur restores the committed value."
    },
    {
      "name": "Composer",
      "purpose": "The prompt input \u2014 a textarea in a form, with optional single-choice modes.",
      "notFor": [
        "Ordinary multi-line form fields"
      ],
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "placeholder": {
          "type": "string",
          "default": "Ask anything"
        },
        "value": {
          "type": "string"
        },
        "onValueChange": {
          "type": "(value: string) => void"
        },
        "onSubmit": {
          "type": "(value: string) => void"
        },
        "modes": {
          "type": "ComposerMode[]",
          "note": "Rendered as a radio group, not buttons."
        },
        "activeMode": {
          "type": "string"
        },
        "onModeChange": {
          "type": "(id: string) => void"
        },
        "tools": {
          "type": "ReactNode",
          "note": "Icon buttons still need labels."
        },
        "submitLabel": {
          "type": "string",
          "default": "Ask"
        },
        "busy": {
          "type": "boolean",
          "default": false
        },
        "disabled": {
          "type": "boolean",
          "default": false
        },
        "hint": {
          "type": "ReactNode",
          "note": "The place for an accuracy notice."
        },
        "maxRows": {
          "type": "number",
          "default": 8
        }
      },
      "note": "Enter submits, Shift+Enter adds a line \u2014 and the form still submits the ordinary way, so switch access and voice control work without knowing the key handler."
    },
    {
      "name": "DatePicker",
      "purpose": "A native date input, with the format and range stated in the description.",
      "notFor": [
        "A custom calendar grid",
        "A duration \u2014 that is two fields"
      ],
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "granularity": {
          "type": "'date' | 'month' | 'week' | 'datetime-local'",
          "default": "date"
        },
        "min": {
          "type": "string",
          "note": "Named in the hint automatically."
        },
        "max": {
          "type": "string"
        },
        "error": {
          "type": "string"
        },
        "DateRangePicker.legend": {
          "type": "string",
          "required": true
        }
      },
      "exports": [
        "DatePicker",
        "DateRangePicker"
      ]
    },
    {
      "name": "DescriptionList",
      "purpose": "Term and value pairs \u2014 a record read rather than a form filled. A real <dl>, so a screen reader knows which label owns which value.",
      "notFor": [
        "A form \u2014 these are values, not inputs",
        "Two columns of unrelated data"
      ],
      "props": {
        "items": {
          "type": "DescriptionItem[]",
          "required": true,
          "note": "{ term, value, wide? }. `wide` spans both columns in layout=columns."
        },
        "layout": {
          "type": "'rows' | 'columns'",
          "default": "columns",
          "note": "rows stacks term above value \u2014 right in a narrow panel. columns puts the term in a gutter so values align and can be read down."
        },
        "density": {
          "type": "'comfortable' | 'compact'",
          "default": "comfortable"
        },
        "ariaLabel": {
          "type": "string",
          "note": "Makes it a labelled group. Omit inside an already-labelled region \u2014 a nameless group is noise."
        }
      },
      "exports": [
        "DescriptionList"
      ]
    },
    {
      "name": "Dialog",
      "purpose": "A decision that must block the rest of the page. Native <dialog>.",
      "notFor": [
        "Non-blocking feedback \u2014 use Toast"
      ],
      "props": {
        "open": {
          "type": "boolean",
          "required": true
        },
        "onClose": {
          "type": "() => void",
          "required": true
        },
        "title": {
          "type": "string",
          "required": true
        },
        "description": {
          "type": "ReactNode"
        },
        "footer": {
          "type": "ReactNode",
          "note": "Primary action last."
        },
        "dismissible": {
          "type": "boolean",
          "default": true,
          "note": "false blocks Escape and backdrop click. Destructive confirmations only."
        },
        "size": {
          "type": "'sm' | 'md' | 'lg'",
          "default": "md"
        }
      }
    },
    {
      "name": "Divider",
      "purpose": "A line between things. Decorative unless it carries a label.",
      "notFor": [
        "Spacing \u2014 use the gap tokens"
      ],
      "props": {
        "orientation": {
          "type": "'horizontal' | 'vertical'",
          "default": "horizontal"
        },
        "children": {
          "type": "ReactNode",
          "note": "Turns it into a labelled role=separator."
        },
        "spacing": {
          "type": "'none' | 'snug' | 'loose'",
          "default": "snug"
        }
      }
    },
    {
      "name": "IconPicker",
      "purpose": "Choosing one icon from a searchable grid. A radio group, not a wall of buttons.",
      "notFor": [
        "An icon set with no names \u2014 the name is the accessible label"
      ],
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "icons": {
          "type": "IconOption[]",
          "required": true,
          "note": "Each needs a name; keywords widen the search."
        },
        "value": {
          "type": "string"
        },
        "onChange": {
          "type": "(name: string) => void",
          "required": true
        },
        "searchable": {
          "type": "boolean",
          "default": true
        },
        "columns": {
          "type": "number",
          "default": 8
        }
      }
    },
    {
      "name": "Input",
      "purpose": "Single-line text entry with label, description and error wired to ARIA.",
      "notFor": [
        "Controls with their own keyboard model \u2014 date pickers, comboboxes"
      ],
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "hideLabel": {
          "type": "boolean",
          "default": false,
          "note": "Hides visually, keeps it for assistive tech."
        },
        "description": {
          "type": "ReactNode",
          "note": "Persists when an error appears."
        },
        "error": {
          "type": "string",
          "note": "Its presence is what sets aria-invalid."
        }
      }
    },
    {
      "name": "List",
      "purpose": "A vertical list of records: leading media, title, supporting line, trailing slot.",
      "notFor": [
        "Data the user compares or sorts \u2014 that is Table",
        "A selectable master pane \u2014 that is ListView"
      ],
      "props": {
        "ariaLabel": {
          "type": "string"
        },
        "dividers": {
          "type": "boolean",
          "default": true
        },
        "ListItem.title": {
          "type": "ReactNode",
          "required": true
        },
        "ListItem.href": {
          "type": "string",
          "note": "Makes the row a link. Mutually exclusive with onSelect."
        },
        "ListItem.onSelect": {
          "type": "() => void",
          "note": "Makes the row a button."
        },
        "ListItem.trailing": {
          "type": "ReactNode",
          "note": "Rendered outside the row's own control."
        }
      },
      "exports": [
        "List",
        "ListItem"
      ]
    },
    {
      "name": "ListView",
      "purpose": "A selectable master list driving a detail pane. One tab stop, arrows, type-ahead.",
      "notFor": [
        "Multi-select \u2014 that is a Table with a checkbox column",
        "Static records \u2014 that is List"
      ],
      "props": {
        "ariaLabel": {
          "type": "string",
          "required": true
        },
        "items": {
          "type": "ListViewItem[]",
          "required": true
        },
        "value": {
          "type": "string | null",
          "note": "Controlled selection."
        },
        "onChange": {
          "type": "(id: string) => void"
        },
        "empty": {
          "type": "ReactNode",
          "note": "Use StateBlock for anything richer than a line."
        }
      }
    },
    {
      "name": "Menu",
      "purpose": "A list of actions behind a trigger. Focus moves in on open and back on close.",
      "notFor": [
        "Navigation \u2014 those are links",
        "Choosing a value \u2014 that is Select"
      ],
      "props": {
        "label": {
          "type": "ReactNode",
          "required": true
        },
        "items": {
          "type": "MenuItem[]",
          "required": true,
          "note": "tone='critical' for destructive items."
        },
        "align": {
          "type": "'start' | 'end'",
          "default": "start"
        },
        "renderTrigger": {
          "type": "(props) => ReactNode",
          "note": "For an icon button or an avatar trigger."
        }
      }
    },
    {
      "name": "NavList",
      "purpose": "Grouped list inside a side panel \u2014 saved views, recent items, a shallow tree.",
      "notFor": [
        "Tabular data \u2014 use Table"
      ],
      "usage": "<NavGroup label=\"Pinned\"><NavItem item={\u2026} active onSelect={\u2026} /></NavGroup>",
      "exports": [
        "NavList",
        "NavGroup",
        "NavItem"
      ],
      "props": {
        "item": {
          "type": "NavListItem",
          "required": true,
          "note": "On NavItem."
        },
        "active": {
          "type": "boolean",
          "default": false
        },
        "onSelect": {
          "type": "(id: string) => void"
        },
        "label": {
          "type": "string",
          "note": "On NavGroup \u2014 wired with aria-labelledby."
        },
        "ariaLabel": {
          "type": "string",
          "note": "On NavList."
        }
      },
      "note": "A real <ul>, so a screen reader announces the item count before the user commits to reading it."
    },
    {
      "name": "NavRail",
      "purpose": "Narrow icon navigation rail.",
      "notFor": [
        "Destinations whose meaning needs more than two words"
      ],
      "props": {
        "items": {
          "type": "NavRailItem[]",
          "required": true
        },
        "activeId": {
          "type": "string"
        },
        "onSelect": {
          "type": "(id: string) => void"
        },
        "footerItems": {
          "type": "NavRailItem[]",
          "note": "Pinned to the bottom."
        },
        "showLabels": {
          "type": "boolean",
          "default": false,
          "note": "Prefer it past about five destinations."
        },
        "ariaLabel": {
          "type": "string",
          "note": "Names the landmark."
        }
      },
      "note": "NavRailItem.label is required \u2014 it is the accessible name, the tooltip and the optional caption. An icon-only rail without it is a memory test."
    },
    {
      "name": "Pagination",
      "purpose": "Paging over a set too large to render. Reports a range and a total, because '21\u201340 of 312' is a fact about the data and 'page 2 of 16' is a fact about the component.",
      "notFor": [
        "Infinite scroll",
        "Fewer rows than one page holds \u2014 then show them all"
      ],
      "props": {
        "page": {
          "type": "number",
          "required": true,
          "note": "Zero-based, so it indexes the data without arithmetic at the call site."
        },
        "pageSize": {
          "type": "number",
          "required": true
        },
        "total": {
          "type": "number",
          "note": "Rows in the whole set. Omit for a cursor-paged endpoint that does not know."
        },
        "onPageChange": {
          "type": "(page: number) => void",
          "required": true
        },
        "pageSizeOptions": {
          "type": "number[]",
          "note": "Omit to hide the size control."
        },
        "onPageSizeChange": {
          "type": "(pageSize: number) => void"
        },
        "unit": {
          "type": "string",
          "default": "results",
          "note": "What is counted: 'invoices', 'documents'."
        },
        "busy": {
          "type": "boolean",
          "default": false,
          "note": "Disables both controls while a request is in flight."
        }
      },
      "exports": [
        "Pagination"
      ]
    },
    {
      "name": "Progress",
      "purpose": "How far along something is. Determinate or honestly indeterminate.",
      "notFor": [
        "A fake bar that waits at 90%",
        "A skeleton's job \u2014 an unknown layout"
      ],
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "value": {
          "type": "number",
          "note": "Omit for the indeterminate state."
        },
        "max": {
          "type": "number",
          "default": 100
        },
        "valueText": {
          "type": "string",
          "note": "e.g. '12 of 40 files'."
        },
        "tone": {
          "type": "'default' | 'success' | 'critical'",
          "default": "default"
        },
        "hideLabel": {
          "type": "boolean",
          "default": false
        }
      }
    },
    {
      "name": "RadioGroup",
      "purpose": "One choice out of a visible, mutually exclusive set. The group is the component.",
      "notFor": [
        "More than about six options \u2014 that is Select or Combobox",
        "Independent toggles \u2014 that is Checkbox"
      ],
      "props": {
        "legend": {
          "type": "string",
          "required": true
        },
        "options": {
          "type": "RadioOption[]",
          "required": true
        },
        "value": {
          "type": "string"
        },
        "onChange": {
          "type": "(value: string) => void"
        },
        "orientation": {
          "type": "'vertical' | 'horizontal'",
          "default": "vertical"
        },
        "error": {
          "type": "string"
        }
      }
    },
    {
      "name": "ScrollArea",
      "purpose": "A scrollable region that is reachable by keyboard and keeps the platform scrollbar.",
      "notFor": [
        "Replacing the scrollbar with divs",
        "A region that never overflows"
      ],
      "props": {
        "ariaLabel": {
          "type": "string",
          "required": true,
          "note": "Used once the region becomes focusable."
        },
        "maxHeight": {
          "type": "number | string",
          "default": 240
        },
        "axis": {
          "type": "'vertical' | 'horizontal' | 'both'",
          "default": "vertical"
        },
        "fade": {
          "type": "boolean",
          "default": true
        }
      },
      "note": "tabIndex is added only while the content actually overflows."
    },
    {
      "name": "SegmentedControl",
      "purpose": "Two to five mutually exclusive options, all visible. A radio group that looks like buttons.",
      "notFor": [
        "More than five options",
        "A filter with a cleared state"
      ],
      "props": {
        "legend": {
          "type": "string",
          "required": true,
          "note": "Visually hidden unless showLegend."
        },
        "showLegend": {
          "type": "boolean",
          "default": false
        },
        "options": {
          "type": "SegmentedOption[]",
          "required": true
        },
        "value": {
          "type": "string",
          "required": true
        },
        "onChange": {
          "type": "(value: string) => void",
          "required": true
        },
        "size": {
          "type": "'sm' | 'md'",
          "default": "md"
        }
      }
    },
    {
      "name": "Select",
      "purpose": "Choosing one value from a short, known list. Native element underneath.",
      "notFor": [
        "Search",
        "Multi-select",
        "Async or very long option lists"
      ],
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "options": {
          "type": "Array<{ value: string; label: string; disabled?: boolean }>",
          "required": true
        },
        "placeholder": {
          "type": "string",
          "note": "Rendered as a disabled first option."
        },
        "description": {
          "type": "ReactNode"
        },
        "error": {
          "type": "string"
        }
      }
    },
    {
      "name": "SidePanel",
      "purpose": "A persistent panel beside the main content.",
      "notFor": [
        "Anything blocking \u2014 use Dialog",
        "Transient drawers"
      ],
      "props": {
        "ariaLabel": {
          "type": "string",
          "required": true,
          "note": "Names the landmark."
        },
        "side": {
          "type": "'start' | 'end'",
          "default": "start"
        },
        "title": {
          "type": "ReactNode"
        },
        "header": {
          "type": "ReactNode",
          "note": "Pinned above the scroll area."
        },
        "footer": {
          "type": "ReactNode",
          "note": "Pinned below it."
        },
        "children": {
          "type": "ReactNode",
          "required": true
        },
        "width": {
          "type": "number",
          "default": 260
        },
        "collapsed": {
          "type": "boolean",
          "default": false
        },
        "onToggle": {
          "type": "() => void"
        }
      },
      "note": "Never traps focus and never makes the page inert \u2014 the user works in main and reads the panel at the same time."
    },
    {
      "name": "Skeleton",
      "purpose": "A placeholder for content whose shape is already known.",
      "notFor": [
        "Waits with no known shape \u2014 use StateBlock state=\"loading\""
      ],
      "usage": "Wrap bones in <SkeletonGroup label=\"Loading documents\">; the group makes the single announcement.",
      "exports": [
        "Skeleton",
        "SkeletonGroup"
      ],
      "props": {
        "width": {
          "type": "number | string",
          "default": "100%"
        },
        "height": {
          "type": "number | string",
          "default": 16
        },
        "radius": {
          "type": "'control' | 'surface' | 'pill'",
          "default": "control"
        },
        "animated": {
          "type": "boolean",
          "default": true,
          "note": "Shimmer is off under prefers-reduced-motion regardless."
        },
        "label": {
          "type": "string",
          "required": true,
          "note": "On SkeletonGroup."
        }
      },
      "note": "Individual bones are aria-hidden; the group announces once. Otherwise a screen reader reads \"blank\" forty times."
    },
    {
      "name": "Slider",
      "purpose": "A native range input for an approximate value on a continuum.",
      "notFor": [
        "A value the user needs to type exactly \u2014 that is Input",
        "Fewer than about ten steps"
      ],
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "value": {
          "type": "number",
          "required": true
        },
        "onChange": {
          "type": "(value: number) => void",
          "required": true
        },
        "min": {
          "type": "number",
          "default": 0
        },
        "max": {
          "type": "number",
          "default": 100
        },
        "formatValue": {
          "type": "(value: number) => string",
          "note": "Becomes aria-valuetext \u2014 '24' alone means nothing."
        }
      }
    },
    {
      "name": "StateBlock",
      "purpose": "The empty, loading and error surfaces of any data view.",
      "notFor": [
        "The happy path"
      ],
      "props": {
        "state": {
          "type": "'empty' | 'loading' | 'error'",
          "required": true
        },
        "title": {
          "type": "string",
          "required": true,
          "note": "One line, sentence case, no trailing period."
        },
        "description": {
          "type": "ReactNode",
          "note": "Say what the user can do, not what the server did."
        },
        "action": {
          "type": "ReactNode",
          "note": "The single most useful next step. Omit rather than invent."
        },
        "secondaryAction": {
          "type": "ReactNode"
        },
        "icon": {
          "type": "ReactNode",
          "note": "Decorative."
        },
        "density": {
          "type": "'inline' | 'section' | 'page'",
          "default": "section"
        },
        "diagnostics": {
          "type": "ReactNode",
          "note": "Error only. Rendered inside a collapsed <details>."
        }
      },
      "announcements": {
        "loading": "role=status \u2014 polite, must not interrupt",
        "error": "role=alert \u2014 assertive, the user's action failed",
        "empty": "no live region \u2014 a successful response with nothing in it"
      }
    },
    {
      "name": "StatusBar",
      "purpose": "The thin bar at the bottom of an application. Its message slot is a polite live region.",
      "notFor": [
        "Anything urgent \u2014 that is Toast",
        "Actions \u2014 that is Toolbar"
      ],
      "props": {
        "items": {
          "type": "StatusBarItem[]",
          "note": "Label and value pairs, read out together."
        },
        "message": {
          "type": "ReactNode",
          "note": "Announced politely. Always rendered so the region exists before it fills."
        },
        "end": {
          "type": "ReactNode"
        },
        "ariaLabel": {
          "type": "string",
          "default": "Status"
        }
      }
    },
    {
      "name": "Switch",
      "purpose": "An immediate setting. Takes effect on click, with no Save button.",
      "notFor": [
        "A value submitted with a form \u2014 that is Checkbox"
      ],
      "props": {
        "label": {
          "type": "ReactNode",
          "required": true
        },
        "checked": {
          "type": "boolean",
          "required": true
        },
        "onChange": {
          "type": "(checked: boolean) => void",
          "required": true
        },
        "description": {
          "type": "ReactNode"
        },
        "labelPosition": {
          "type": "'start' | 'end'",
          "default": "end"
        }
      },
      "note": "role=switch, so the state announces as on/off rather than checked."
    },
    {
      "name": "Table",
      "purpose": "Dense enterprise data. Sort and paging are controlled, empty/loading/error delegate to StateBlock, and the narrow form is declared rather than guessed.",
      "notFor": [
        "Layout"
      ],
      "props": {
        "caption": {
          "type": "string",
          "required": true,
          "note": "Names the table for assistive tech."
        },
        "columns": {
          "type": "Array<Column<Row>>",
          "required": true
        },
        "rows": {
          "type": "Row[]",
          "required": true
        },
        "rowKey": {
          "type": "(row: Row) => string",
          "required": true
        },
        "sort": {
          "type": "{ key: string; direction: 'ascending' | 'descending' }",
          "note": "Controlled \u2014 the same component works with server-side sorting."
        },
        "onSortChange": {
          "type": "(sort) => void"
        },
        "state": {
          "type": "StateBlockProps",
          "note": "Replaces the body with a StateBlock across every column."
        },
        "responsive": {
          "type": "'scroll' | 'stack' | 'priority'",
          "default": "scroll",
          "note": "scroll keeps the grid and scrolls the container \u2014 right when the reader is comparing across columns. stack turns each row into term/value pairs via DescriptionList \u2014 right at six columns or fewer. priority moves low-priority columns into a per-row disclosure \u2014 right for a wide table with one identifying column. Queries are @container, so it responds to its own width and works in a SidePanel."
        },
        "selection": {
          "type": "{ selected: Set<string>, onChange, label? }",
          "note": "Checkbox column plus a tri-state select-all. Partial resolves to 'select the rest', never to 'clear'."
        },
        "rowActions": {
          "type": "(row) => MenuItem[]",
          "note": "Per-row overflow Menu, which already solves focus return."
        },
        "stickyHeader": {
          "type": "boolean",
          "default": false,
          "note": "Column meaning is lost the moment the header scrolls away."
        },
        "density": {
          "type": "'comfortable' | 'compact'",
          "default": "comfortable"
        },
        "totals": {
          "type": "(rows) => ReactNode[]",
          "note": "Rendered in <tfoot>, so a sort cannot move it into the data."
        },
        "Column.numeric": {
          "type": "boolean",
          "note": "Aligns end and sets tabular-nums. Two things always wanted together."
        },
        "Column.priority": {
          "type": "1 | 2 | 3",
          "default": 1,
          "note": "What survives when the container narrows, under responsive=priority. Declared by whoever knows the domain."
        },
        "Column.hideWhenStacked": {
          "type": "boolean",
          "note": "For a column with no value to label, such as actions."
        }
      }
    },
    {
      "name": "Tabs",
      "purpose": "A filter over one screen. Roving tabindex, arrows to move, panel is the next tab stop.",
      "notFor": [
        "Content with its own URL \u2014 those are links",
        "More tabs than fit without scrolling"
      ],
      "props": {
        "defaultTab": {
          "type": "string",
          "note": "Defaults to the first tab registered."
        },
        "value": {
          "type": "string",
          "note": "Controlled selection."
        },
        "onChange": {
          "type": "(id: string) => void"
        },
        "manual": {
          "type": "boolean",
          "default": false,
          "note": "Arrows move focus; Enter or Space activates. Use for expensive panels."
        },
        "TabList.ariaLabel": {
          "type": "string",
          "required": true
        },
        "Tab.id": {
          "type": "string",
          "required": true
        },
        "TabPanel.id": {
          "type": "string",
          "required": true
        }
      },
      "exports": [
        "Tabs",
        "TabList",
        "Tab",
        "TabPanel"
      ]
    },
    {
      "name": "Toast",
      "purpose": "Ephemeral confirmation that something happened.",
      "notFor": [
        "Errors the user must act on in place \u2014 use StateBlock"
      ],
      "usage": "Wrap the app in <ToastProvider>, then call useToast().notify(...).",
      "props": {
        "tone": {
          "type": "'info' | 'success' | 'warning' | 'critical'",
          "note": "critical routes to the assertive live region and never auto-dismisses."
        },
        "title": {
          "type": "string",
          "required": true
        },
        "description": {
          "type": "string"
        },
        "duration": {
          "type": "number | null",
          "default": "5000, or null for critical"
        }
      }
    },
    {
      "name": "Toolbar",
      "purpose": "A row of controls acting on the content below it. One tab stop, arrows between controls.",
      "notFor": [
        "Page-level navigation \u2014 that is AppBar",
        "A single button"
      ],
      "props": {
        "ariaLabel": {
          "type": "string",
          "required": true,
          "note": "Names what the toolbar acts on."
        },
        "children": {
          "type": "ReactNode",
          "required": true
        },
        "end": {
          "type": "ReactNode",
          "note": "Trailing slot: search, view switches, counts."
        },
        "orientation": {
          "type": "'horizontal' | 'vertical'",
          "default": "horizontal"
        },
        "flush": {
          "type": "boolean",
          "default": false
        }
      }
    },
    {
      "name": "Tooltip",
      "purpose": "A short label for a control that has none. Dismissible, hoverable, persistent.",
      "notFor": [
        "Anything interactive inside it \u2014 that is a popover",
        "Text the user must read to succeed"
      ],
      "props": {
        "content": {
          "type": "ReactNode",
          "required": true,
          "note": "Plain text."
        },
        "children": {
          "type": "ReactElement",
          "required": true,
          "note": "One focusable element."
        },
        "placement": {
          "type": "'top' | 'bottom'",
          "default": "top"
        },
        "delay": {
          "type": "number",
          "default": 200,
          "note": "Hover only. Focus shows it immediately."
        }
      },
      "note": "Describes the trigger via aria-describedby; it never becomes its accessible name."
    },
    {
      "name": "UserProfile",
      "purpose": "Who is signed in, and the account actions. Name as text, not only as initials.",
      "notFor": [
        "Showing another person \u2014 that is Avatar",
        "App navigation"
      ],
      "props": {
        "name": {
          "type": "string",
          "required": true
        },
        "secondary": {
          "type": "string",
          "note": "E-mail or tenant \u2014 what tells two accounts apart."
        },
        "items": {
          "type": "MenuItem[]",
          "note": "Sign out last. Without them it renders as static identity."
        },
        "compact": {
          "type": "boolean",
          "default": false,
          "note": "Hides the text; the button then names the account itself."
        },
        "badge": {
          "type": "ReactNode"
        }
      }
    }
  ],
  "rules": [
    "Never reference a primitive token from product code.",
    "Never use a spacing value outside the 4px scale.",
    "Never write a bespoke empty, loading or error state \u2014 use StateBlock.",
    "Never let colour be the only carrier of meaning.",
    "Never remove a focus outline; add className=\"bh-focusable\" instead.",
    "Never use a placeholder in place of a label.",
    "Prefer a new component over a new prop on an existing one.",
    "Never rebuild the application frame out of divs \u2014 AppShell carries the named landmarks.",
    "Never ship a drag-only interaction; BoardCard.onMove is the pointer-free path (WCAG 2.5.1).",
    "Never use a Skeleton for a wait whose shape is unknown \u2014 it promises a layout that never arrives.",
    "Never leave a SidePanel or NavRail without an ariaLabel.",
    "Never use role=\"toolbar\", \"tablist\" or \"listbox\" without the arrow-key handling they promise \u2014 Toolbar, Tabs and ListView already implement it.",
    "Never put an interactive element inside a Tooltip; a tooltip the user has to reach is a popover.",
    "Never use a Switch for a value that is submitted with a form, or a Checkbox for one that takes effect immediately.",
    "Never let a Combobox commit free text \u2014 the value must come from the list.",
    "Never build a scrollable region without a keyboard path to it; ScrollArea adds the tab stop only while it overflows."
  ],
  "templates": {
    "note": "Templates are documented in Storybook and deliberately not exported. Copy them into the product and edit.",
    "available": [
      {
        "name": "AI Chat",
        "pattern": "Conversational analysis workspace",
        "states": [
          "ready",
          "loading",
          "error",
          "returning user"
        ]
      },
      {
        "name": "Kanban board",
        "pattern": "Documents moving through stages",
        "states": [
          "ready",
          "over WIP limit",
          "empty columns",
          "loading",
          "error"
        ]
      }
    ]
  }
}
