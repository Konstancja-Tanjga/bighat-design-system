{
  "name": "@bighatpoland/ui",
  "version": "2.0.0",
  "skill": "./agent/SKILL.md",
  "storybook": "https://bighatpoland.github.io/bighat-design-system/",
  "tokens": {
    "layers": ["primitive", "semantic"],
    "productApi": "semantic",
    "cssPrefix": "--bh",
    "note": "Product code and generated code may reference semantic tokens only. Primitives exist to define semantics and are not part of the public API.",
    "semanticGroups": [
      "surface",
      "text",
      "border",
      "action",
      "status",
      "control",
      "gap",
      "padding",
      "radius",
      "elevation"
    ],
    "contrast": {
      "standard": "WCAG 2.1 AA",
      "bodyText": 4.5,
      "largeText": 3,
      "nonText": 3,
      "enforcedBy": "src/tokens/contrast.test.ts — runs in CI, fails the build"
    }
  },
  "components": [
    {
      "name": "AppBar",
      "purpose": "The top bar. Three slots in reading order: brand, title, actions.",
      "notFor": ["Repeating the product name in title — brand already carries it"],
      "props": {
        "brand": {
          "type": "ReactNode",
          "note": "Logo or wordmark."
        },
        "title": {
          "type": "ReactNode",
          "note": "Names the current screen, not the product."
        },
        "titleAsHeading": {
          "type": "boolean",
          "default": true,
          "note": "false when the screen has its own visible <h1>."
        },
        "center": {
          "type": "ReactNode",
          "note": "Search or filters acting on this screen."
        },
        "actions": {
          "type": "ReactNode",
          "note": "Primary action last."
        }
      }
    },
    {
      "name": "AppShell",
      "purpose": "The frame every application screen sits in. Owns the landmark structure — header, nav, main, complementary.",
      "notFor": ["Documentation and marketing pages — use height=\"flow\" or no shell at all"],
      "props": {
        "header": {
          "type": "ReactNode",
          "note": "Rendered as <header>."
        },
        "rail": {
          "type": "ReactNode",
          "note": "Narrow icon rail on the leading edge."
        },
        "sidebar": {
          "type": "ReactNode",
          "note": "Leading panel — navigation, a tree."
        },
        "aside": {
          "type": "ReactNode",
          "note": "Trailing panel — inspector, context."
        },
        "children": {
          "type": "ReactNode",
          "required": true,
          "note": "Becomes the only <main>."
        },
        "height": {
          "type": "'fill' | 'flow'",
          "default": "fill",
          "meaning": "fill pins to the viewport and scrolls each region independently"
        }
      },
      "exports": ["AppShell", "SkipLink"],
      "note": "Slots are optional. A screen without a rail is this component with a prop left out, not a second layout."
    },
    {
      "name": "Badge",
      "purpose": "Short status attached to an object.",
      "notFor": ["Bare counts", "Anything clickable"],
      "props": {
        "tone": {
          "type": "'neutral' | 'info' | 'success' | 'warning' | 'critical'",
          "default": "neutral"
        },
        "dot": {
          "type": "boolean",
          "default": false,
          "note": "Decorative. The label still carries the meaning."
        },
        "children": {
          "type": "ReactNode",
          "required": true,
          "note": "Required by design — colour alone fails WCAG 1.4.1."
        }
      }
    },
    {
      "name": "Board",
      "purpose": "Columns of cards — kanban, pipeline, review queue.",
      "notFor": ["Ordered data the user compares — that is Table"],
      "props": {
        "Board.ariaLabel": {
          "type": "string",
          "required": true
        },
        "Board.announcement": {
          "type": "string",
          "note": "Spoken by the board's live region after a move."
        },
        "BoardColumn.title": {
          "type": "string",
          "required": true
        },
        "BoardColumn.count": {
          "type": "number",
          "required": true
        },
        "BoardColumn.limit": {
          "type": "number",
          "note": "WIP limit — surfaced, never enforced."
        },
        "BoardColumn.empty": {
          "type": "ReactNode",
          "note": "Use a StateBlock with copy specific to the column."
        },
        "BoardCard.moveTargets": {
          "type": "Array<{id,label}>"
        },
        "BoardCard.onMove": {
          "type": "(targetId: string) => void",
          "note": "Required for a pointer-free move — WCAG 2.5.1."
        }
      }
    },
    {
      "name": "Button",
      "purpose": "An action the user takes.",
      "notFor": ["Navigation — use an anchor styled as a link"],
      "props": {
        "variant": {
          "type": "'primary' | 'secondary' | 'ghost'",
          "default": "primary",
          "meaning": "visual weight"
        },
        "tone": {
          "type": "'default' | 'critical'",
          "default": "default",
          "meaning": "consequence of the action"
        },
        "size": {
          "type": "'sm' | 'md' | 'lg'",
          "default": "md"
        },
        "loading": {
          "type": "boolean",
          "default": false,
          "note": "Stays focusable; blocks the click."
        },
        "loadingLabel": {
          "type": "string",
          "default": "Loading"
        },
        "iconStart": {
          "type": "ReactNode",
          "note": "Decorative — hidden from assistive tech."
        },
        "iconEnd": {
          "type": "ReactNode"
        },
        "fullWidth": {
          "type": "boolean",
          "default": false
        }
      },
      "deprecated": {
        "variant=\"danger\"": {
          "replacement": "variant=\"primary\" tone=\"critical\"",
          "removedIn": "3.0.0",
          "reason": "variant conflated visual weight with consequence"
        }
      }
    },
    {
      "name": "Card",
      "purpose": "A bounded surface around content the user might act on as a unit.",
      "notFor": [
        "A paragraph — that is a border with extra steps",
        "Cards with two actions inside"
      ],
      "props": {
        "children": {
          "type": "ReactNode",
          "required": true
        },
        "onClick": {
          "type": "() => void",
          "note": "Makes the whole surface one button."
        },
        "ariaLabel": {
          "type": "string",
          "note": "Its accessible name when onClick is set."
        },
        "elevation": {
          "type": "'flat' | 'raised'",
          "default": "flat"
        },
        "accent": {
          "type": "'none' | 'info' | 'success' | 'warning' | 'critical'",
          "default": "none",
          "note": "Decorative — never the only carrier of a status."
        },
        "padding": {
          "type": "'snug' | 'normal'",
          "default": "normal"
        },
        "dragging": {
          "type": "boolean",
          "default": false
        }
      }
    },
    {
      "name": "Composer",
      "purpose": "The prompt input — a textarea in a form, with optional single-choice modes.",
      "notFor": ["Ordinary multi-line form fields"],
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "placeholder": {
          "type": "string",
          "default": "Ask anything"
        },
        "value": {
          "type": "string"
        },
        "onValueChange": {
          "type": "(value: string) => void"
        },
        "onSubmit": {
          "type": "(value: string) => void"
        },
        "modes": {
          "type": "ComposerMode[]",
          "note": "Rendered as a radio group, not buttons."
        },
        "activeMode": {
          "type": "string"
        },
        "onModeChange": {
          "type": "(id: string) => void"
        },
        "tools": {
          "type": "ReactNode",
          "note": "Icon buttons still need labels."
        },
        "submitLabel": {
          "type": "string",
          "default": "Ask"
        },
        "busy": {
          "type": "boolean",
          "default": false
        },
        "disabled": {
          "type": "boolean",
          "default": false
        },
        "hint": {
          "type": "ReactNode",
          "note": "The place for an accuracy notice."
        },
        "maxRows": {
          "type": "number",
          "default": 8
        }
      },
      "note": "Enter submits, Shift+Enter adds a line — and the form still submits the ordinary way, so switch access and voice control work without knowing the key handler."
    },
    {
      "name": "Dialog",
      "purpose": "A decision that must block the rest of the page. Native <dialog>.",
      "notFor": ["Non-blocking feedback — use Toast"],
      "props": {
        "open": {
          "type": "boolean",
          "required": true
        },
        "onClose": {
          "type": "() => void",
          "required": true
        },
        "title": {
          "type": "string",
          "required": true
        },
        "description": {
          "type": "ReactNode"
        },
        "footer": {
          "type": "ReactNode",
          "note": "Primary action last."
        },
        "dismissible": {
          "type": "boolean",
          "default": true,
          "note": "false blocks Escape and backdrop click. Destructive confirmations only."
        },
        "size": {
          "type": "'sm' | 'md' | 'lg'",
          "default": "md"
        }
      }
    },
    {
      "name": "Input",
      "purpose": "Single-line text entry with label, description and error wired to ARIA.",
      "notFor": ["Controls with their own keyboard model — date pickers, comboboxes"],
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "hideLabel": {
          "type": "boolean",
          "default": false,
          "note": "Hides visually, keeps it for assistive tech."
        },
        "description": {
          "type": "ReactNode",
          "note": "Persists when an error appears."
        },
        "error": {
          "type": "string",
          "note": "Its presence is what sets aria-invalid."
        }
      }
    },
    {
      "name": "NavList",
      "purpose": "Grouped list inside a side panel — saved views, recent items, a shallow tree.",
      "notFor": ["Tabular data — use Table"],
      "usage": "<NavGroup label=\"Pinned\"><NavItem item={…} active onSelect={…} /></NavGroup>",
      "exports": ["NavList", "NavGroup", "NavItem"],
      "props": {
        "item": {
          "type": "NavListItem",
          "required": true,
          "note": "On NavItem."
        },
        "active": {
          "type": "boolean",
          "default": false
        },
        "onSelect": {
          "type": "(id: string) => void"
        },
        "label": {
          "type": "string",
          "note": "On NavGroup — wired with aria-labelledby."
        },
        "ariaLabel": {
          "type": "string",
          "note": "On NavList."
        }
      },
      "note": "A real <ul>, so a screen reader announces the item count before the user commits to reading it."
    },
    {
      "name": "NavRail",
      "purpose": "Narrow icon navigation rail.",
      "notFor": ["Destinations whose meaning needs more than two words"],
      "props": {
        "items": {
          "type": "NavRailItem[]",
          "required": true
        },
        "activeId": {
          "type": "string"
        },
        "onSelect": {
          "type": "(id: string) => void"
        },
        "footerItems": {
          "type": "NavRailItem[]",
          "note": "Pinned to the bottom."
        },
        "showLabels": {
          "type": "boolean",
          "default": false,
          "note": "Prefer it past about five destinations."
        },
        "ariaLabel": {
          "type": "string",
          "note": "Names the landmark."
        }
      },
      "note": "NavRailItem.label is required — it is the accessible name, the tooltip and the optional caption. An icon-only rail without it is a memory test."
    },
    {
      "name": "Select",
      "purpose": "Choosing one value from a short, known list. Native element underneath.",
      "notFor": ["Search", "Multi-select", "Async or very long option lists"],
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "options": {
          "type": "Array<{ value: string; label: string; disabled?: boolean }>",
          "required": true
        },
        "placeholder": {
          "type": "string",
          "note": "Rendered as a disabled first option."
        },
        "description": {
          "type": "ReactNode"
        },
        "error": {
          "type": "string"
        }
      }
    },
    {
      "name": "SidePanel",
      "purpose": "A persistent panel beside the main content.",
      "notFor": ["Anything blocking — use Dialog", "Transient drawers"],
      "props": {
        "ariaLabel": {
          "type": "string",
          "required": true,
          "note": "Names the landmark."
        },
        "side": {
          "type": "'start' | 'end'",
          "default": "start"
        },
        "title": {
          "type": "ReactNode"
        },
        "header": {
          "type": "ReactNode",
          "note": "Pinned above the scroll area."
        },
        "footer": {
          "type": "ReactNode",
          "note": "Pinned below it."
        },
        "children": {
          "type": "ReactNode",
          "required": true
        },
        "width": {
          "type": "number",
          "default": 260
        },
        "collapsed": {
          "type": "boolean",
          "default": false
        },
        "onToggle": {
          "type": "() => void"
        }
      },
      "note": "Never traps focus and never makes the page inert — the user works in main and reads the panel at the same time."
    },
    {
      "name": "Skeleton",
      "purpose": "A placeholder for content whose shape is already known.",
      "notFor": ["Waits with no known shape — use StateBlock state=\"loading\""],
      "usage": "Wrap bones in <SkeletonGroup label=\"Loading documents\">; the group makes the single announcement.",
      "exports": ["Skeleton", "SkeletonGroup"],
      "props": {
        "width": {
          "type": "number | string",
          "default": "100%"
        },
        "height": {
          "type": "number | string",
          "default": 16
        },
        "radius": {
          "type": "'control' | 'surface' | 'pill'",
          "default": "control"
        },
        "animated": {
          "type": "boolean",
          "default": true,
          "note": "Shimmer is off under prefers-reduced-motion regardless."
        },
        "label": {
          "type": "string",
          "required": true,
          "note": "On SkeletonGroup."
        }
      },
      "note": "Individual bones are aria-hidden; the group announces once. Otherwise a screen reader reads \"blank\" forty times."
    },
    {
      "name": "StateBlock",
      "purpose": "The empty, loading and error surfaces of any data view.",
      "notFor": ["The happy path"],
      "props": {
        "state": {
          "type": "'empty' | 'loading' | 'error'",
          "required": true
        },
        "title": {
          "type": "string",
          "required": true,
          "note": "One line, sentence case, no trailing period."
        },
        "description": {
          "type": "ReactNode",
          "note": "Say what the user can do, not what the server did."
        },
        "action": {
          "type": "ReactNode",
          "note": "The single most useful next step. Omit rather than invent."
        },
        "secondaryAction": {
          "type": "ReactNode"
        },
        "icon": {
          "type": "ReactNode",
          "note": "Decorative."
        },
        "density": {
          "type": "'inline' | 'section' | 'page'",
          "default": "section"
        },
        "diagnostics": {
          "type": "ReactNode",
          "note": "Error only. Rendered inside a collapsed <details>."
        }
      },
      "announcements": {
        "loading": "role=status — polite, must not interrupt",
        "error": "role=alert — assertive, the user's action failed",
        "empty": "no live region — a successful response with nothing in it"
      }
    },
    {
      "name": "Table",
      "purpose": "Comparable rows of structured data.",
      "notFor": ["Layout"],
      "props": {
        "caption": {
          "type": "string",
          "required": true,
          "note": "Names the table for assistive tech."
        },
        "columns": {
          "type": "Array<Column<Row>>",
          "required": true
        },
        "rows": {
          "type": "Row[]",
          "required": true
        },
        "rowKey": {
          "type": "(row: Row) => string",
          "required": true
        },
        "sort": {
          "type": "{ key: string; direction: 'ascending' | 'descending' }",
          "note": "Controlled — the same component works with server-side sorting."
        },
        "onSortChange": {
          "type": "(sort) => void"
        },
        "state": {
          "type": "StateBlockProps",
          "note": "Replaces the body with a StateBlock across every column."
        }
      }
    },
    {
      "name": "Toast",
      "purpose": "Ephemeral confirmation that something happened.",
      "notFor": ["Errors the user must act on in place — use StateBlock"],
      "usage": "Wrap the app in <ToastProvider>, then call useToast().notify(...).",
      "props": {
        "tone": {
          "type": "'info' | 'success' | 'warning' | 'critical'",
          "note": "critical routes to the assertive live region and never auto-dismisses."
        },
        "title": {
          "type": "string",
          "required": true
        },
        "description": {
          "type": "string"
        },
        "duration": {
          "type": "number | null",
          "default": "5000, or null for critical"
        }
      }
    }
  ],
  "rules": [
    "Never reference a primitive token from product code.",
    "Never use a spacing value outside the 4px scale.",
    "Never write a bespoke empty, loading or error state — use StateBlock.",
    "Never let colour be the only carrier of meaning.",
    "Never remove a focus outline; add className=\"bh-focusable\" instead.",
    "Never use a placeholder in place of a label.",
    "Prefer a new component over a new prop on an existing one.",
    "Never rebuild the application frame out of divs — AppShell carries the named landmarks.",
    "Never ship a drag-only interaction; BoardCard.onMove is the pointer-free path (WCAG 2.5.1).",
    "Never use a Skeleton for a wait whose shape is unknown — it promises a layout that never arrives.",
    "Never leave a SidePanel or NavRail without an ariaLabel."
  ],
  "templates": {
    "note": "Templates are documented in Storybook and deliberately not exported. Copy them into the product and edit.",
    "available": [
      {
        "name": "AI Chat",
        "pattern": "Conversational analysis workspace",
        "states": ["ready", "loading", "error", "returning user"]
      },
      {
        "name": "Kanban board",
        "pattern": "Documents moving through stages",
        "states": ["ready", "over WIP limit", "empty columns", "loading", "error"]
      }
    ]
  }
}
